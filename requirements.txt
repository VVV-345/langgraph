# =============================================================================
# 自主 AI 编码代理 —— 核心依赖
# =============================================================================
# 安装方式：
#   pip install -r requirements.txt
#
# 注意：langchain-huggingface 是可选的，仅在使用本地嵌入模型时需要。
# 如需本地嵌入，取消注释下面这行：
# langchain-huggingface>=0.1
# =============================================================================

python-dotenv>=1.0
langchain-core>=0.3
langgraph>=0.2
langchain-openai>=0.2
pydantic>=2.0
